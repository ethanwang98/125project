<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a125project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DataDisplay"></activity>
        <activity android:name=".MainMenu" />
        <activity
            android:name=".YesNoActivity"
            android:label="@string/title_activity_yes_no"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ScaleQuestionActivity"
            android:label="@string/title_activity_scale_question"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".DropDownMenuActivity"
            android:label="DropDownMenuActvitiy"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ResultActivity"
            android:label="@string/title_activity_result"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>